(function(){function setupCarousel(root){const track=root.querySelector("[data-preview-track]");const slides=Array.from(root.querySelectorAll("[data-preview-slide]"));const dots=Array.from(root.querySelectorAll("[data-preview-dot]"));const prev=root.querySelector("[data-preview-prev]");const next=root.querySelector("[data-preview-next]");if(!track||slides.length===0)return;function activeIndex(){const tr=track.getBoundingClientRect();const center=tr.left+tr.width/2;let best=0,dist=Infinity;slides.forEach((slide,i)=>{const r=slide.getBoundingClientRect();const c=r.left+r.width/2;const d=Math.abs(c-center);if(d<dist){dist=d;best=i}});return best}function updateDots(){const i=activeIndex();dots.forEach((dot,n)=>{dot.classList.toggle("is-active",n===i);dot.setAttribute("aria-current",n===i?"true":"false")})}function goTo(i){const target=slides[Math.max(0,Math.min(i,slides.length-1))];if(target){target.scrollIntoView({behavior:"smooth",block:"nearest",inline:"center"})}}prev&&prev.addEventListener("click",()=>goTo(activeIndex()-1));next&&next.addEventListener("click",()=>goTo(activeIndex()+1));dots.forEach((dot,i)=>dot.addEventListener("click",()=>goTo(i)));track.addEventListener("scroll",()=>window.requestAnimationFrame(updateDots),{passive:true});window.addEventListener("resize",updateDots);updateDots()}document.addEventListener("DOMContentLoaded",()=>{document.querySelectorAll("[data-preview-carousel]").forEach(setupCarousel)})})();